fn main () {
	// Assign the number we want to interrate to
	max := 10

	// Set the interator
	mut i := 0

	// Loop and set the condition
	for i <= max {
		// Print the interation
		println('Interation: $max/$i')

		// Up the interator with one
		i++
	}
}